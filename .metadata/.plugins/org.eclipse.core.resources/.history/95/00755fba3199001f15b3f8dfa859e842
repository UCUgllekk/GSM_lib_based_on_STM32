/*
 * GSM_MODULE.hpp
 *
 *  Created on: Nov 2, 2024
 *      Author: gllekk
 */

#ifndef INC_GSMMODULE_HPP_
#define INC_GSMMODULE_HPP_

class gsmmodule{
	struct Parameters{
		Pin pin;
		//
	};
public:
		gsmmodule(const Parameters& parameters);
		int send_sms(const std::string& message);
		void make_call();
		bool send_AT();
private:
		int send_at_command(const std::string& command);
		Parameters parameters;
		// other private
};

#endif /* INC_GSMMODULE_HPP_ */
